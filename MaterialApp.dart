MaterialApp(
      theme: ThemeData(
        primarySwatch: Colors.deepOrange
      ),
  );
  
 when you want to utilize this color somewhere else in your app then you can use like
 just instance:
 color:Theme.of(context).primaryColor
